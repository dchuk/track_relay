// Stub — replaced in Task 2 with the real init/track implementation.
// Task 1 only needs something that tsup can build so the build smoke
// test can confirm the dual ESM+CJS pipeline produces real artifacts.
export const init = () => {};
export const track = () => {};
